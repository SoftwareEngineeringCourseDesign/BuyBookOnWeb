module.exports = {
  dev: {
    host: 'localhost',
    port: 4400, // local port for webpack-server
  },
  api: { // api proxy
    host: 'http://bbow.qiankaihua.top', // api host
    prefix: 'api'
  }
}
